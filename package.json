{
  "name": "airbnb-capstone",
  "private": true,
  "version": "1.0.0",
  "description": "Airbnb capstone — API, public client, and admin (Heroku monorepo)",
  "license": "ISC",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "npm install --prefix client --include=dev && npm run build --prefix client && npm install --prefix admin --include=dev && npm run build --prefix admin -- --base /admin/ && npm install --prefix backend",
    "seed": "npm run seed --prefix backend",
    "test:api": "npm run test:api --prefix backend"
  },
  "cacheDirectories": [
    "client/node_modules",
    "admin/node_modules",
    "backend/node_modules"
  ]
}
